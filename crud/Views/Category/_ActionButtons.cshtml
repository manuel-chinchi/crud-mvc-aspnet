@model crud.Models.Category

@*@using (Html.BeginForm(null, null, FormMethod.Get, new { @action = "/Category/Delete/" + Model.Id }))
{
    <button type="button" class="btn btn-danger btn-sm" data-toggle="modal" data-target="#modal_delete_@{@Model.Id}">Eliminar</button>
}*@

@{
    if (Model.Articles.Count == 0)
    {
        using (Html.BeginForm("Delete", "Category", FormMethod.Get, null))
        {
            <button type="button" class="btn btn-danger btn-sm text-white" data-toggle="modal" data-target="#modal_delete_@{@Model.Id}"><span class="fa fa-trash"></span></button>
        }
    }
    else
    {
        <button class="btn btn-warning btn-sm text-white" disabled><span class="fa fa-exclamation-circle"></span></button>
    }
}

@Html.Partial("_Delete", Model)